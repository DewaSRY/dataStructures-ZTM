import { describe, it } from "vitest";
import { findPair } from "./DynamicProgramming";
describe("component ", () => {
  it("fi", () => {
    console.log(findPair([-4, 4], -8));
    console.log("dew".substring(1));
  });
  it.todo("should have some property ");
  it.todo("should have some property ");
  it.todo("should have some property ");
  it.todo("should have some property ");
});
